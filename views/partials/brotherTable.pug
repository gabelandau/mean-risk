box.box
    div.content
        h4.title.is-4 Instructions
        ol
            li Verify all values for brothers are correct. If they are not, click edit and edit the form accordingly.
            li To remove a brother from the draft temporarily (if they are exempt from the current draft for whatever reason) click the remove button. They will be put back into the draft when the page is refreshed.
            li To run the draft, fill in values on the left form (or use the default values) and click Run Draft.

box.box.brother-box
    h4.title.is-4.table-header Draftable Brothers
    table.table.is-striped
        thead
            tr
                th Scroll
                th Name
                th Points
                th Draft Order
                th
        tfoot
            tr
                th Scroll
                th Name
                th Points
                th Draft Order
                th
        tbody
            tr(ng-repeat="x in brothers")
                td &#35;{{x.initiation_number}}
                td {{x.name}}
                td {{x.points}}
                td {{x.draft_order}}
                td.edit
                    button.button.is-danger(ng-click="tempExemption($index)") Remove
                    button.button(ng-click="openEditBrotherForm('draftable', $index)") Edit

box.box.brother-box
    h4.title.is-4.table-header Exempt Brothers
    table.table.is-striped
        thead
            tr
                th Scroll
                th Name
                th Points
                th Co-Op?
                th EC?
                th Graduating?
                th
        tfoot
            tr
                th Scroll
                th Name
                th Points
                th Co-Op?
                th EC?
                th Graduating?
                th
        tbody
            tr(ng-repeat="x in exempt_brothers")
                td &#35;{{x.initiation_number}}
                td {{x.name}}
                td {{x.points}}
                td(style="color:#f00") {{x.coop ? "Yes" : ""}}
                td(style="color:#f00") {{x.executive_council ? "Yes" : ""}}
                td(style="color:#f00") {{x.senior ? "Yes" : ""}}
                td.edit
                    button.button(ng-click="openEditBrotherForm('exempt', $index)") Edit
